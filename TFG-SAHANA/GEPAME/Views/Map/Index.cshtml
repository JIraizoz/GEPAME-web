@{
    ViewData["Title"] = "Map";
}
<section id="mapa">
    <p>Hola, esto es una prueba</p>
    <div id="mapid" style="height:500px;"></div>
</section>
<script>

    var mymap = L.map('mapid').setView([42.4604, -2.4453], 13);

    L.tileLayer('https://api.tiles.mapbox.com/v4/{id}/{z}/{x}/{y}.png?access_token=pk.eyJ1IjoibWFwYm94IiwiYSI6ImNpejY4NXVycTA2emYycXBndHRqcmZ3N3gifQ.rJcFIG214AriISLbB6B5aw', {
        maxZoom: 18,
        attribution: 'Map data &copy; <a href="http://openstreetmap.org">OpenStreetMap</a> contributors, ' +
            '<a href="http://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, ' +
            'Imagery © <a href="http://mapbox.com">Mapbox</a>',
        id: 'mapbox.streets'
    }).addTo(mymap);

    L.marker([42.46671, -2.42285]).addTo(mymap)
        .bindPopup("<b>Hello world!</b><br />I am a popup.");
    L.marker([42.4646690, -2.4457818]).addTo(mymap)
        .bindPopup("<b>Espartero</b><br>'Tienes los huevos más grandes que el caballo de Espartero'.");

    

    var popup = L.popup();

    function onMapClick(e) {
        popup
            .setLatLng(e.latlng)
            .setContent("You clicked the map at " + e.latlng.toString())
            .openOn(mymap);
    }

    mymap.on('contextmenu', onMapClick);

    //var mymap = L.map('mapid').setView([42.4604, -2.4453], 14);

    //L.tileLayer('https://api.tiles.mapbox.com/v4/{id}/{z}/{x}/{y}.png?access_token=pk.eyJ1IjoibWFwYm94IiwiYSI6ImNpejY4NXVycTA2emYycXBndHRqcmZ3N3gifQ.rJcFIG214AriISLbB6B5aw', {
    //    maxZoom: 18,
    //    attribution: 'Map data &copy; <a href="http://openstreetmap.org">OpenStreetMap</a> contributors, ' +
    //        '<a href="http://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, ' +
    //        'Imagery © <a href="http://mapbox.com">Mapbox</a>',
    //    id: 'mapbox.streets'
    //}).addTo(mymap);
    //var greenIcon = new LeafIcon({
    //    icon

    //})
    //var marker = L.marker([42.46671, -2.42285]).addTo(mymap);
    //marker.bindPopup("<b>Hello world!</b><br>I am a popup.");
    //var marker2 = L.marker([42.4646690, -2.4457818]).addTo(mymap);
    //marker2.bindPopup("<b>Espartero</b><br>'Tienes los huevos más grandes que el caballo de Espartero'.");

    //function onMapClick(e) {
    //    popup
    //        .setLatLng(e.latlng)
    //        .setContent("You clicked the map at " + e.latlng.toString())
    //        .openOn(mymap);
    //}

    //mymap.on('click', onMapClick);

</script>